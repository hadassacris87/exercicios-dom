let positivos = 0;

for (let i = 1; i <= 5; i++) {
    let numero = parseInt(prompt("Digite um número:"));

    if (numero > 0) {
        positivos++;
    }
}

console.log("Quantidade de números positivos: " + positivos);
